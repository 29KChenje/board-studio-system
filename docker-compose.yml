services:
  app:
    build:
      context: .
      dockerfile: server/Dockerfile
    environment:
      PORT: 5000
      CLIENT_URL: http://localhost:5000
      JWT_SECRET: change_me
      UPLOAD_DIR: uploads
    ports:
      - "5000:5000"
